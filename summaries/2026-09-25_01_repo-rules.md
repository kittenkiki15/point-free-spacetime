# 2026-09-25 第 01 回: リポジトリの運用ルール

- 対話ログ: [logs/2026-09-25_01_repo-rules.md](../logs/2026-09-25_01_repo-rules.md)

## 要約

プロジェクトの最初のセッションとして、公開リポジトリ `point-free-spacetime` と非公開リポジトリ `point-free-spacetime-private` の運用ルールを、対話を通して決めた。
あわせて、Lean 4 + Mathlib による形式証明と、Python による数値実験を、このクラウド環境と GitHub Actions の両方で実行できることを確認した。
後半は、PR のたびに動くクロスモデルレビュー（`gpt-6-sol`）の仕組みを、不具合と安全性の問題に対処しながら作り直した。最初の PR（[#1](https://github.com/kittenkiki15/point-free-spacetime/pull/1)）は `main` にマージ済み。

## 決定事項

### リポジトリの構成

- 公開リポジトリ（CC0 1.0）に、対話ログ（`logs/`）、まとめ（`summaries/`）、調査メモ（`surveys/`）、用語一覧（`glossary.md`）、参考文献一覧（`references.bib`）、予想（`conjectures/`）、Lean のコード（`lean/`）、Python のコード（`sim/`）を置く。
- 非公開リポジトリに、論文の PDF（`papers/<引用キー>.pdf`）と公開に向かないメモ（`notes/`）を置く。引用キーは公開側の `references.bib` と揃える。

### 検証

- 対話で出てきた定理・命題は、可能なものを Lean 4 + Mathlib で形式的に検証し、そのコードを公開する。
- 物理モデルのシミュレーションなどによる検証は Python で行い、そのコードを公開する。
- Lean が保証するのは形式化した命題の正しさだけなので、形式化が元の主張を正しく表しているかは別に確認する。

### 予想の管理

- 未検証の主張は「予想」として 1 件 1 ファイルで管理する。
- 確度・重要度・検証費用を「高・中・低」の段階で評価する。優先度は対話の中で決める。
- 予想の内容と評価はファイルで、作業の進み具合と議論は GitHub の Issue で管理する。

### 対話ログ

- セッション記録を変換した生ログ（個人情報などは除去）と、整形したまとめの両方を置く。

### 著作権・公開範囲

- 公開側には他者の文章の長い引用、図、PDF を置かない。書誌情報と短い引用にとどめる。

### 言語と表記

- 文書は日本語を基本とし、用語一覧では英語の原語を併記する。
- 数式は、ユーザーが Firefox と Chrome で表示を確かめてまとめた [`docs/math-guide.md`](../docs/math-guide.md) に従う。

### Git の運用

- 作業ブランチから `main` への PR にする。
- PR のたびに、OpenAI の `gpt-6-sol` による AI コードレビュー（クロスモデルレビュー）を GitHub Actions で自動実行する。
  - 対話の生ログ（`logs/`）は後から修正しないので、レビューの対象外とする。
- コミットメッセージは日本語で書く。
- セッションの終わりに、毎回ログとまとめをコミットする。
- セッションの後はコンテキストを `/clear` で初期化するので、次にやることを [`NEXT.md`](../NEXT.md) に残す。

### クロスモデルレビューの仕組み（試行錯誤の結果）

| 段階 | 方式 | 問題 | 対処 |
| --- | --- | --- | --- |
| 1 | 公式の `openai/codex-action` | レビューは完了するが、ステップが終了せず結果を投稿できない | Codex CLI を直接実行し、時間の上限を付ける |
| 2 | Codex CLI（読み取り専用サンドボックス） | サンドボックス（bwrap）が GitHub のランナー（Ubuntu 24.04）で起動せず、ファイルを読めない | 制限の解除は安全性を下げるため見送り、資料と差分をプロンプトに含める |
| 3 | Codex CLI（プロンプトに差分を含める） | レビューで「モデルが API キーを読めるおそれ」を指摘された | モデルにツールを与えず、Responses API を直接呼ぶ |
| 4 | Responses API を直接呼ぶ（`pull_request`） | レビューで「PR 側のワークフローが Secret を扱える」を指摘された | `pull_request_target` で起動し、Secret を使う処理は `main` の版だけで実行する |

- 最終的な方式では、PR の内容は差分のデータとしてだけ読み、PR のコードは実行しない。
- レビューの仕組みの変更は、`main` にマージされた後の PR から反映される。
- 途中の版のレビューが 2 回動き、計 14 件の指摘に対応した。主なもの:
  - 対話ログの伏せ字を強化した（AWS のキー、秘密鍵、大文字の UUID など）。ツール入力はシェルのコマンドだけを残す。テストを `tools/tests/` に追加した。
  - 予想の状態に「数値的に支持」を設け、「証明済み」は数学的な証明がある場合に限った。
  - ロケールを「点を持たない」ではなく「点を基本データとしない」構造と説明するよう改めた。
  - 1 件（数式ガイドの表の `\|`）は、Markdown の表のエスケープであり誤検知と判断した。

### 対話ログの伏せ字

- 個人情報の断片など、公開リポジトリに書けない語句は、非公開リポジトリの `redactions.txt` に置き、変換スクリプトから読み込む。
- 当初、確認用の検索コマンドに個人情報の断片を直接書いたため、それがログに残りかけた。検索には `redactions.txt` を使うことにした。

## 得られた結果（検証済み）

- このクラウド環境で Lean 4（v4.34.1）と Mathlib を使えることを確認した。
  - ネットワーク設定で `releases.lean-lang.org`・`cache.mathlib.org` などを許可した。
  - `reservoir.lean-lang.org` にはアクセスできないため、Mathlib は `lakefile.toml` で GitHub の URL を直接指定する。
  - Mathlib のビルド済みキャッシュの取得には約 2 分かかる。
- Mathlib を使って、フレームにおける分配律（有限の交わりが任意の結びに対して分配すること）$`a ⊓ ⨆_i\, f_i = ⨆_i\, (a ⊓ f_i)`$ と、その特別な場合である二項の分配律 $`a ⊓ (b ⊔ c) = (a ⊓ b) ⊔ (a ⊓ c)`$ を検証した（[`lean/PointFreeSpacetime/Basic.lean`](../lean/PointFreeSpacetime/Basic.lean)）。Mathlib にロケール（`Locale`）が定義されていることも確認した。
- `lake build --wfail` により、`sorry` を含む証明がビルド失敗になることを確認した。

## 新しい予想

（なし）

## 未解決の論点

- 最終的な方式のクロスモデルレビューは、まだ実際の PR で動作を確認していない（次の PR で確認する）。
- 本題（点なし時空による物理学）の出発点をどこに置くか。

## 次にやること

[`NEXT.md`](../NEXT.md) を参照。
