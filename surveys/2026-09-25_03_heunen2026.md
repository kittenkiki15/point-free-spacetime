# heunen2026: Causal coverage in ordered locales and spacetimes

- 書誌情報: Chris Heunen, Nesta van der Schaaf, *Causal coverage in ordered locales and spacetimes*, Journal of Mathematical Physics 67(8), 083506, 2026. DOI: [10.1063/5.0332267](https://doi.org/10.1063/5.0332267)、arXiv: 2510.17417
- 入手状況: 非公開リポジトリに PDF あり。出版社版（`heunen2026.pdf`、第 04 回に入手）と arXiv 版 v1（`heunen2026_arxiv-v1.pdf`、2025-10-20）。**以下の節・定理番号は arXiv 版 v1 のもの**で、出版社版との違いは未確認（出版社版は節番号がローマ数字）
- 作成: 2026-09-25（第 03 回）
- 関連: [heunen2024 のメモ](2026-09-25_03_heunen2024.md)、[基礎の調査メモ](2026-09-25_02_pointfree-topology-basics.md)

本文を通読して書いた。論文にない Claude の解釈や見通しには **（見立て）** と付ける。
記号は heunen2024 のメモに合わせる（開集合の順序 $`⊴`$、局所的な錐 $`↟`$・$`↡`$、開集合の包含 $`⊑`$）。

## 要旨（自分の言葉で）

順序付きロケールの上で、相対論の因果論の道具（因果的な被覆、依存領域）を点を使わずに定義し、滑らかな時空で従来の曲線による定義と比べる。
Christensen–Crane の「因果サイト」（christensen2005）の因果被覆の考えを改良したものである。

### 動機（1 節）

- 点なしの時空を支持する議論として、座標の精密な測定には無限の精度が要ること、Isham と Sorkin の「物理的に意味があるのは点ではなく開集合」という主張、連続な時空の上の粒子の位置の確率（ルベーグ測度に関して連続な測度）では、一点の確率は波動関数によらず常に 0 なので、意味のある確率を持つのは領域であること（Arntzenius。論文はこれを「時空の可測集合を零集合で割った完備ブール代数は**アトムを持たない**」と言い換えている。離散的な測定結果のように、一点が正の確率を持つ場合の話ではない。なお、この言い換えは heunen2026 のもので、arntzenius2003 自身は完備性にもアトムにも触れていない。下の「その他」を参照）、場の量子論では一点での場は観測量にならないこと（Haag）を挙げる。

### 平行な順序付きロケール（2〜3 節）

- 順序付きロケールの公理（heunen2024 の (∨)）に加えて、次を課す。
  - (c-⊴)：$`U ⊴ V \iff U ⊑ ↡V \text{ かつ } V ⊑ ↟U`$。二つの閉包作用素（モナド）$`↟`$・$`↡`$ から順序が決まる、という条件。
  - (c-∨)：局所的な錐 $`↟`$・$`↡`$ が任意の結びを保つ。上順序だけの順序付きロケールでは空の結びについてすら成り立たず、開錐条件のない空間では有限の結びでも成り立たない例がある（例 3.3、3.4）。
  - **平行性**（parallel orderedness）：公理 (∧±) と、空集合が自分自身としか関係しないという公理 (∅) の組。(∧±) は、(c-⊴) のもとで次の不等式 (f±) と同値である（補題 3.7）。

```math
↟U ∧ V ⊑ ↟(U ∧ ↡V), \qquad ↡U ∧ V ⊑ ↡(U ∧ ↟V)
```

  帰結として、**$`↟U ∧ V = ∅ \iff U ∧ ↡V = ∅`$**（命題 3.10）。未来へ伝わる速さと過去へ伝わる速さが等しいことを表す、と説明されている。未来と過去で光速が異なる $`ℝ^2`$ の例では、この性質が破れる（例 3.13）。
- 開錐条件を満たす順序付き空間から作った順序付きロケールは、(c-∨) と平行性を満たす（命題 3.2、3.11）。**滑らかな時空は開錐条件を満たし**（定理 2.9）、$`↟U = I^+(U) = J^+(U)`$、$`↡U = I^-(U) = J^-(U)`$ となる（系 2.10）。したがって滑らかな時空から作った順序付きロケールは、これらの公理をすべて満たす。
- 光速 0 のミンコフスキー時空にあたる「vertical-$`ℝ^2`$」も、時空でない平行な順序付きロケールの例である。

### 経路と因果被覆（4〜5 節）

- **経路**（path）：空でない開集合の有限列 $`p_0 ⊴ p_1 ⊴ ⋯ ⊴ p_N`$。閉区間 $`[0, 1]`$ 上の連続な単調曲線を、その像を覆う開集合の列で近似したものとみなす。この場合、像はコンパクトなので有限列で足りる（過去に延長不能な曲線のように、像がコンパクトとは限らない曲線は、この近似の対象ではない）。
- 経路の**細分**（refinement）、終点の部分領域への**制限**（構成 4.4：平行性を使って後ろから順に $`(p|_W)_n = p_n ∧ ↡(p|_W)_{n+1}`$ と定める）、**局所的な過去細分**（終点の開被覆ごとに細分をとること）を定義する。
- **因果被覆**（定義 5.2）：$`A`$ が $`U`$ を**下から覆う**とは、$`A ⊑ ↡U`$ であり、$`U`$ に着く任意の経路が、局所的に過去細分して $`A`$ を通るようにできること。「$`U`$ に流れ込む情報はすべて $`A`$ を通る」という直観である。覆う領域の集合を $`\mathrm{Cov}^-_⊴(U)`$ と書く（上から覆う $`\mathrm{Cov}^+_⊴`$ も同様）。
- 性質（命題 5.5、補題 5.6、5.7）：自分自身を覆う、推移性、制限（引き戻し）で安定、$`A ∈ \mathrm{Cov}^-(U)`$ なら $`A ⊴ U`$、結びとの両立 (Cov-∨)。Christensen–Crane の因果サイトでは、引き戻しでの安定性が成り立たないことがあり、この点を改良している。
- 順序が等号のとき、因果被覆は等号になる（例 5.4）。

### グロタンディーク位相としての因果被覆（6 節）

- 通常のグロタンディーク位相は、各対象の上の「覆う篩」を指定する。因果被覆では、覆う領域が $`U`$ と交わらない（$`U`$ の過去にある）ので、$`U`$ 上の篩ではなく $`↡U`$ 上の篩を考える必要がある。
- 自己関手 $`T`$ と自然変換 $`μ : T^2 → T`$ を使った **$`(T, μ)`$-グロタンディーク位相**を定義し（定義 6.4）、$`\mathrm{Cov}^-_⊴`$ が $`↡`$-グロタンディーク位相を定めることを示す（定理 6.5）。順序が等号のときは、通常の標準的な位相に戻る（例 6.6）。
- クライスリ圏の上の通常のグロタンディーク位相としては扱えない、という注意もある。

### 時空での比較（7 節）

- 時空の曲線による被覆 $`\mathrm{Caus}^-`$（因果曲線）と $`\mathrm{Chron}^-`$（時間的曲線）を定義し、$`\mathrm{Caus}^-(U) ⊆ \mathrm{Chron}^-(U) ⊆ \mathrm{Cov}^-_⊴(U)`$ を示す（命題 7.7）。
- 逆は成り立たない：覆う領域から一本の時間的曲線の像を除いても、点なしの経路はその「無限に細い穴」を通れないので、点なしの意味では覆ったままである（注意 7.8）。著者らは、情報を無限に細い所へ押し込める経路を除外する点なしの定義のほうが物理的に適切だと論じている。

### 抽象的な因果被覆（8 節）

- 順序を使わずに、因果被覆そのものを公理 (C1)〜(C5) で定義し、それを持つロケールを（Christensen–Crane とは別の意味の）**因果サイト**とよぶ（定義 8.1）。
- 因果被覆から結びを保つモナド $`L^±`$（影響領域）が得られ、(c-∨) を満たす平行な順序付きロケールから作った因果被覆では、元の順序が復元される（補題 8.3、命題 8.5）。
- 逆向き（抽象的な因果被覆から順序付きロケールを作り、再び因果被覆を作ると元に戻るか）は未解決で、フレームとサイトの随伴（Ball–Pultr）に類する対応を予想している。

### 依存領域（9〜10 節）

- **依存領域**（domain of dependence）：$`A`$ が下から覆う最大の開集合 $`D^+(A)`$。モナドになるが、結びは保たない（補題 9.2）。因果被覆は、影響領域 $`L^±`$ と依存領域 $`D^∓`$ から決まる（補題 9.3）。
- 時空では、従来の依存領域 $`D^+_≼`$（過去に延長不能な因果曲線がすべて $`A`$ と交わる点）、$`D^+_≪`$（時間的曲線版）、有界な曲線による $`D^+_{\mathrm{Caus}}`$・$`D^+_{\mathrm{Chron}}`$、点なしの $`D^+_⊴`$ を比べ、一般に次の包含が成り立つことを示す（10 節）。

```math
D^+_≼ ⊆ D^+_≪ ⊆ D^+_{\mathrm{Chron}} ⊆ D^+_⊴, \qquad D^+_≼ ⊆ D^+_{\mathrm{Caus}} ⊆ D^+_{\mathrm{Chron}}
```

  どの包含にも、真の包含になる例がある（等しくなる場合もある。たとえば $`A`$ が時空全体なら、どれも時空全体になる）。違いは、時空から点や閉領域を除いたとき（延長不能な曲線と有界な曲線の違い）と、$`A`$ から曲線を除いたとき（曲線と点なしの経路の違い）に現れる。
- 従来の依存領域は非時間的（achronal）な集合、たとえば空間的超曲面の上で考えるが、開集合 $`U`$ で $`↟U ∧ ↡U = ∅`$ となるのは $`U = ∅`$ のときだけなので、順序付きロケールの開集合ではこれが自明になる。部分ロケールの上の因果順序が必要だと述べている。

### 今後の課題として挙げられていること（11 節）

- **決定論的な層**：因果被覆についての層条件は「過去のデータが未来のデータへ決定論的に発展する」と解釈できる。例として、ミンコフスキー時空上の波動方程式の解の前層（初期データが依存領域での解を一意に決める）。ただし定義 6.4 の一般化されたグロタンディーク位相での層条件の正確な定式化は未解決。
- **時空の穴**（hole-freeness）：点や閉領域を除いた時空は穴を持つが、Krasnikov によるとミンコフスキー時空すら従来の定義では穴を持つ。点なしの依存領域 $`D^+_⊴`$ でこの問題が解消するか。

## 本プロジェクトとの関係

### 調査メモ 7 節の予想の候補は、既に扱われているか

1. **点なしの時制論理**：**一部は扱われている。**
   - 扱われていること：(c-∨) と平行性を満たす順序付きロケールでは、局所的な錐 $`↟`$（調査メモの過去の ◇、$`P`$）と $`↡`$（未来の ◇、$`F`$）が、結びを保つ閉包作用素になる。結びを保つので右随伴（$`G`$、$`H`$）を持つ。また、平行性の帰結 $`↟U ∧ V = ∅ \iff U ∧ ↡V = ∅`$ が成り立つ。
   - **（見立て）** これは、ブール代数の演算子についての Jónsson–Tarski の**共役**（conjugate）の条件と同じ形であり、時制論理で $`P`$ と $`F`$ が互いに逆の関係から来ることの代数的な表現にあたる。(f±) の $`↟U ∧ V ⊑ ↟(U ∧ ↡V)`$ も、最小の時制論理 Kt の定理 $`Pp ∧ q → P(p ∧ Fq)`$ と同じ形である。
   - **第 04 回の確認**：ブール代数では、結びを保つ演算子について共役と (f±) は同値である。フレームでは次のとおり、**(f±) のほうが真に強い**。したがって、時制論理の基本条件（Kt）の点なし版とみなすべきなのは、共役（命題 3.10 の形）ではなく **(f±) そのもの**であり、命題 3.10 はその帰結である（ユーザーと合意）。
     - (f±) ならば共役：任意のフレームで成り立つ。$`↟U ∧ V = ∅`$ なら、(f±) の 2 本目から $`↡V ∧ U ⊑ ↡(V ∧ ↟U) = ↡∅ = ∅`$。逆向きも同様。補元は使わない。
     - 共役ならば (f±)：一般のフレームでは成り立たない。5 元の束 $`0 < a < b, c < 1`$ で、$`f`$ を恒等写像、$`g`$ を $`c ↦ 1`$（他は恒等）とすると、0 でない元どうしが必ず交わるので共役の条件は自明に成り立つが、$`g(c) ∧ b = b ⋢ a = g(c ∧ f(b))`$ となり (f±) が破れる。小さい有限分配束を総当たりで調べた結果とあわせて [`sim/src/pfs_sim/conjugate.py`](../sim/src/pfs_sim/conjugate.py) に置いた。
     - 未確認：時空から作ったフレームのような良いクラスでは、共役から (f±) が出るか。上の反例は時空とは程遠い束である。
   - 扱われていないこと：時制論理・様相論理としての**完全性**（どの式が妥当になるか）、Goldblatt の S4.2 に対応する**方向性**（二つの領域が共通の未来を持つ）の条件、点の上での評価と開集合の上での評価の比較。これらは本プロジェクトの予想として残る。
2. **点がないと表せない因果構造**：**別の形で一部が扱われている。**
   - 点なしの依存領域 $`D^+_⊴`$ は、曲線による依存領域を常に含み、真に大きくなる例がある（10 節）。論文の例（注意 7.8）では、覆う領域から曲線一本の像を除いても、点なしの意味では覆ったままである。ただし、ここで使う開集合フレームは測度 0 の集合で割ったものではないので、これは零測度の変更一般を無視するという主張ではない。
   - heunen2024 の例 6.6：時空をロケールに移すと、点の間の因果関係の一部が失われる（一点を除いたミンコフスキー時空）。
   - 扱われていないこと：点なしでしか区別できない因果構造（順序付きロケールとして異なるが、点の空間としては同じもの）の体系的な研究。heunen2024 の例 3.11 のように点を持たない順序付きロケールは作れるので、候補 2 は「点を持たない順序付きロケールの因果的な性質（因果被覆、依存領域など）が、どの点のある時空とも違うか」という形で具体化できる **（見立て）**。
3. **量子と時空の点のなさの統一**：**この 2 本では扱われていない**（他の先行研究は未調査）。 動機として、量子論では点の測度が 0 であること（Arntzenius）や、場の量子論で一点の場が観測量にならないこと（Haag）が挙げられているだけである。heunen2024 の 9 節は、順序付きロケール上の層を文脈依存性の研究に使う可能性と、クォンタールへの一般化に触れている。

### その他

- **基礎の調査メモ 5 節の見立ての確認**：時空の開集合 $`U`$ について $`I^+(U)`$ が開集合で、$`I^+`$ が任意の合併を保つことは、系 2.10 と命題 3.2 で裏づけられた。
- **Lean で形式化する定理との関係**（見立て）：heunen2026 による Arntzenius の観察の言い換えは、ルベーグ測度（時空の体積測度）のように**一点の測度が 0 になる非原子的な測度**を使うとき、可測集合を零集合で割った代数（測度代数）がアトムを持たない、というものである。Lean の定理を適用するには、測度代数が**完備**ブール代数であることも別に必要で、これは測度が σ-有限（より一般に局所化可能）なら成り立つ。ルベーグ測度はどちらの仮定（σ-有限と非原子的）も満たす。この二つの仮定のもとで、「完備ブール代数の点はアトムと対応する」と組み合わせると、その測度代数のロケールは点を持たない、という結論になる。ディラック測度のように原子を持つ測度では、アトムも点もあるので成り立たない。形式化する定理の物理的な使い道の一つである。
- **arntzenius2003 の確認**（第 04 回に原典を読んだ）：Arntzenius 自身の主張は次のとおりで、完備ブール代数やアトムという言葉は使っていない。
  - 1 節：実数直線のボレル集合を、ルベーグ測度 0 の違いで同一視した「Region」の代数を「点のない幾何」（Carathéodory、Skyrms）の例として挙げる。すべての点は空の Region に潰れる。
  - 2 節：可分なヒルベルト空間では、連続な観測量は固有状態を持たない。波動関数は測度 0 の違いで同一視した関数の同値類であり、点のない空間の上の関数とみなせる。
  - 3〜5 節：点の値を再導入する方法（リグド・ヒルベルト空間、Halvorson の特異状態）を検討し、どちらも不自然だと論じる。
  - 6 節：場の量子論では、場の演算子は点ではなく試験関数（の測度 0 の違いで同一視した同値類）で定義されるので、時空も点のない空間とみなせる。
- **Halvorson の特異状態との関係**（見立て、ユーザーと合意して記録）：Arntzenius の 4 節で扱う Halvorson（halvorson2001）の**特異状態**（singular state）は、Region の代数のすべての元に確率 0 か 1 を割り当てる状態で、可算加法性を破る。代数の言葉では、これは測度代数の**超フィルター**（ブール代数の意味の点、2 値の準同型）だが、**完全素フィルター**（ロケールの意味の点）ではない。第 03 回の Lean の定理（完備ブール代数の点はアトムと対応する）が区別するのはちょうどこの差であり、アトムを持たない測度代数では、ロケールの点はないが、超フィルターは（ブール素イデアル定理のもとで）多数ある。Halvorson の点の再導入は後者を使っていることになる。可算加法性の破れは、この超フィルターが可算な結びについて素でない（可算個の元の結びが属するのに、どの元も属さない）ことにあたる。σ-有限な測度の測度代数では、任意の結びが可算個の部分の結びに等しいので、可算な結びについて素であることと完全素であることは同値になり、ロケールの点がないことと特異状態が可算加法性を破ることは表裏一体である。
- **決定論的な層**の考え（11.1 節）は、場の理論を点なしで扱う入口になる。層条件の定式化が未解決なので、本プロジェクトの課題の候補になる。

## 関連する予想・用語

- 用語：平行な順序付きロケール、経路、細分、因果被覆、因果サイト、依存領域、影響領域、グロタンディーク位相、篩、共役な演算子、特異状態、超フィルター（[用語一覧](../glossary.md)）。
- 記号：$`\mathrm{Cov}^±_⊴`$、$`D^±`$、$`L^±`$、$`≪`$、$`≼`$（[記号一覧](../symbols.md)）。
- 予想：まだ登録していない。上の 1〜3 を、次のタスク（物理学での「点」の問題に進み、最初の予想を立てる）で対話の中で評価する。
